on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Azure SQL Deploy
        uses: Azure/sql-action@v1
        with:
          # Name of the Azure SQL Server name, like Fabrikam.database.windows.net.
          server-name: sqldiary.database.windows.net
          # The connection string, including authentication information, for the Azure SQL Server database.
          connection-string: server="sqldiary.database.windows.net";user id="Pau057178";password=${{ secrets.HASLO }}; port=1433; database="dziennik";
